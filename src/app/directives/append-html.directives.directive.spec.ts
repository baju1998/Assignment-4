import { AppendHtmlDirective } from './append-html.directives.directive';

describe('AppendHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new AppendHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
